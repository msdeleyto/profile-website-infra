name: 'Terraform Validate'
description: 'Validates Terraform configuration for a specific environment'

inputs:
  environment:
    description: 'Environment to validate (test/prod)'
    required: true
  aws-region:
    description: 'AWS region'
    required: true
  aws-role-arn:
    description: 'AWS IAM role ARN to assume'
    required: true
  s3-state-bucket:
    description: 'S3 bucket for Terraform state'
    required: true
  s3-state-key:
    description: 'S3 key for Terraform state'
    required: true
  terraform-version:
    description: 'Terraform version to use'
    required: true

outputs:
  results:
    description: 'Combined validation results as JSON'
    value: ${{ steps.results.outputs.json }}

runs:
  using: 'composite'
  steps:
    - name: Setup Terraform and AWS
      uses: ./.github/actions/setup
      with:
        environment: ${{ inputs.environment }}
        aws-region: ${{ inputs.aws-region }}
        aws-role-arn: ${{ inputs.aws-role-arn }}
        s3-state-bucket: ${{ inputs.s3-state-bucket }}
        s3-state-key: ${{ inputs.s3-state-key }}
        terraform-version: ${{ inputs.terraform-version }}

    - name: Terraform Validate
      id: validate
      shell: bash
      working-directory: envs/${{ inputs.environment }}
      run: terraform validate -no-color

    - name: Terraform Plan
      id: plan
      shell: bash
      working-directory: envs/${{ inputs.environment }}
      run: terraform plan -no-color
      continue-on-error: true

    - name: Generate Results JSON
      id: results
      shell: bash
      if: always()
      run: |
        cat << EOF >> $GITHUB_OUTPUT
        json={"validate":"${{ steps.validate.outcome }}","plan":"${{ steps.plan.outcome }}"}
        EOF

    - name: Validation Summary
      shell: bash
      if: always()
      run: |
        echo "## Terraform Validation Results (${{ inputs.environment }})" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "| Check | Status |" >> $GITHUB_STEP_SUMMARY
        echo "|-------|--------|" >> $GITHUB_STEP_SUMMARY
        echo "| Validate | ${{ steps.validate.outcome }} |" >> $GITHUB_STEP_SUMMARY
        echo "| Plan | ${{ steps.plan.outcome }} |" >> $GITHUB_STEP_SUMMARY

    - name: Check for failures
      shell: bash
      if: steps.validate.outcome == 'failure'
      run: exit 1
